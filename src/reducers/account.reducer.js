import types from 'types';

const INITIAL_STATE = {
  account: null,
};

const reducer = (state = INITIAL_STATE, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};

export default reducer;
